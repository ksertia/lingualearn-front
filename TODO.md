# TODO: Corriger les erreurs dans CreateUserDetails.vue

- [x] Fusionner les deux blocs `<script setup lang="ts">` en un seul bloc valide.
- [x] Corriger le computed `formattedDate` pour qu'il soit correctement défini.
- [x] Mettre à jour le template pour utiliser les bonnes propriétés de l'interface `User` (e.g., `user.profile.firstName` au lieu de `user.prenom`).
- [x] Supprimer les duplications et les erreurs de syntaxe.

# TODO: Corriger les erreurs dans api.ts

- [x] Corriger les noms de champs dans `createUser` et `updateUser` pour correspondre à l'interface `User`.
- [x] Supprimer l'import inutilisé de `parcours`.
- [x] Vérifier l'utilisation des composables Nuxt.
